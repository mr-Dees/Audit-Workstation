# CHANGELOG

**Текущая версия: `17.1.3`.**

## Методология

- **MAJOR** — что-то существующее сломано или заменено несовместимо: удалён/изменён публичный REST-эндпоинт,
  переименован/удалён ключ конфигурации, заменён механизм авторизации, инфраструктурная зависимость стала
  обязательной без фолбэка, либо фундаментальный редизайн модели хранения/архитектуры приложения.
- **MINOR** — новая функциональность, ничего не ломающая: новый домен, новый модуль, новая возможность поверх
  существующей структуры.
- **PATCH** — багфиксы, рефакторинг без изменения поведения, тесты, документация, стилевые правки. Отдельно не
  расписаны (не входит в объём этой ревизии), так как изменения частые и входят в состав минора/мажора

## Как поднимать версию

Номер версии хранится **в одном месте** — `__version__` в `app/__init__.py`. `Settings.app_version` импортирует
его оттуда, дальше значение расходится само: health-эндпоинты, OpenAPI, `<meta name="app-version">` и
cache-busting статики (`?v=` из фильтра `versioned`). В `.env` версия не задаётся — `APP_VERSION` в шаблонах
закомментирована намеренно (детали — §9.4.4 в [`docs/guides/deploy-and-configuration.md`](docs/guides/deploy-and-configuration.md)).

**Когда.** Отдельным коммитом **на `master`, сразу после мержа** законченного блока работ (или после
законченной серии мини-коммитов прямо на `master`). В feature-ветках версию не трогают: хеш коммита для записи
известен только после мержа, а параллельные ветки иначе конфликтуют в `CHANGELOG.md` и плодят дубликаты.
Несколько подряд идущих правок одного калибра — один бамп в конце, а не по бампу на коммит.

**Что правится** (ровно два файла):

1. `app/__init__.py` — `__version__`.
2. `CHANGELOG.md` — строка «Текущая версия» вверху + запись: для MAJOR/MINOR отдельный раздел с обоснованием,
   для PATCH — строка `- X.Y.Z · \`hash\` — PATCH: …` под соответствующим разделом.

Дальше `pytest tests/test_version_consistency.py` (сверяет код ↔ CHANGELOG ↔ шаблоны `.env`) и коммит с
сообщением `Версия поднята до X.Y.Z` — без тела, обоснование живёт здесь.

---

## 1.0.0 · `276a710`

Базовая версия: конструктор акта (дерево пунктов), редактор таблиц (объединение/разделение ячеек), предварительный
просмотр, контекстное меню.

| Версия | Коммит | Что добавлено |
|---|---|---|
| 1.1.0 | `4430768` | Текстовые блоки как тип узла дерева |
| 1.2.0 | `35ef536` | Блок «Нарушено/установлено» |
| 1.3.0 | `9fed05d` | Экспорт в DOCX |
| 1.4.0 | `58ea4a2` | Экспорт в Markdown |
| 1.5.0 | `1a54ef9` | Изображения в блоке нарушений (paste/drag) |
| 1.6.0 | `1f7a840` | Централизованная система уведомлений |
| 1.7.0 | `740b875` | Гиперссылки и сноски в текстовых блоках (капсулы) |
| 1.8.0 | `e11b6e5` | Закреплённые (обязательные) таблицы |
| 1.9.0 | `5bbf5ba` | Автоформирование сводных таблиц метрик |
| 1.10.0 | `d99eb00` | Таблицы разбиты на подвиды (риск/регулярная/обычная) |
| 1.11.0 | `fd46922` | Поле «Рекомендации» в блоке нарушения |
| 1.12.0 | `f184748` | Автосохранение в localStorage |

## 2.0.0 · `ed5c7e7` (+`558a0f5`)

**MAJOR: конструктор → сервис.** Появился слой БД (`app/db/`), многоактовое хранение, роутеры актов;
приложение из локального инструмента с файловым экспортом стало сервисом, требующим БД.

| Версия | Коммит | Что добавлено |
|---|---|---|
| 2.1.0 | `5fee378` | Модуль экстракторов для ИИ-ассистента |
| 2.2.0 | `335dac1` | Бургер-меню и меню настроек приложения |
| 2.3.0 | `df3791d` | Карточки актов с участниками и поручениями |
| 2.4.0 | `558a0f5` | Части акта: уникальность КМ → КМ+часть |
| 2.5.0 | `81b6478` | Механизм авторизации |
| 2.6.0 | `5614bae` | Расширенная модель хранения (служебные записки, числовое КМ) |
| 2.7.0 | `d2dc6c9` | Блокировки актов |
| 2.8.0 | `4e65152` | Поддержка Greenplum (второй СУБД) |
| 2.9.0 | `2479f9b` | Адаптация под JupyterHub/datalab-прокси («1 сервер — много клиентов») |
| 2.10.0 | `72a0ec3` | Стартовая страница-портал |
| 2.11.0 | `0d9d397` | Прототип чата |
| 2.12.0 | `86c22eb` | Подключение ассистентов к чату |
| 2.13.0 | `43e47e3` | Страницы ЦК Фин.Рез и ЦК Клиентский опыт |
| 2.14.0 | `ef41e87` | Контроль доступа к акту |
| 2.15.0 | `4d94042` | Атрибут территориального банка у пунктов акта |
| 2.16.0 | `c1a1b05` | Прикрепление фактур к акту |
| 2.17.0 | `62084f9` | Единый сервис audit_id |
| 2.18.0 | `c667814` | ИИ-ассистент на странице конструктора |

## 3.0.0 · `904c83c`

**MAJOR: сервис → доменный сервис.** Весь бэкенд перенесён из общих `app/api/v1/endpoints` в плагин-домены
`app/domains/*` с динамической регистрацией (`domain_registry`/`settings_registry`) — 137 файлов.

| Версия | Коммит | Что добавлено |
|---|---|---|
| 3.1.0 | `4ae55ad` | Версионирование содержимого акта (журнал изменений, diff) |
| 3.2.0 | `92de2a0` | Ролевая модель доступа и домен `admin` |
| 3.3.0 | `0765235` | Поиск и добавление внешних пользователей |
| 3.4.0 | `f6780f5` | Autocomplete участников аудиторской группы |
| 3.5.0 | `72d188f` | Домен `ua_data` (справочники) |
| 3.6.0 | `7d9cf2a` | Домен `ck_fin_res` (backend) |
| 3.7.0 | `998362c` | Домен `ck_client_exp` (backend) |
| 3.8.0 | `d5b7ef4` | Страница портала «ЦК Клиентский опыт» |
| 3.9.0 | `fb354d2` | Страница портала «ЦК Фин.Рез» |
| 3.10.0 | `7103557` | Аудит-лог администрирования |

## 4.0.0 · `3e5e708`

**MAJOR: чат — эндпоинт заменён.** Единственный эндпоинт `POST /api/v1/chat/message` удалён, вместо него —
API бесед/сообщений/файлов с оркестратором и SSE-стримингом.

| Версия | Коммит | Что добавлено |
|---|---|---|
| 4.1.0 | `e26001f` | Файлы в чате: drag-and-drop, предпросмотр, лимиты |
| 4.2.0 | `4112922` | Автоперестройка разделов 1–2 дерева при смене типа проверки |
| 4.3.0 | `74faa04` | Профили LLM-провайдера (openrouter/sglang) |
| 4.4.0 | `09660da` | Интеграция с ИИ-агентом БЗ ОАРБ (мост к внешнему агенту) |
| 4.5.0 | `b385cb5` | Тип блока чата `client_action` |
| 4.6.0 | `71be0b2` | Инструмент `chat.forward_to_knowledge_agent` |
| 4.7.0 | `7536e9a` | Инструмент `acts.open_act_page` (навигация из чата) |
| 4.8.0 | `64408a8` | Per-domain инструменты открытия страниц, `chat.list_pages` |

## 5.0.0 · `c7e811a`

**MAJOR: смена префикса таблиц.** Все таблицы переименованы под общий префикс; `DATABASE__GP__TABLE_PREFIX`
удалена и заменена `DATABASE__TABLE_PREFIX` — требует правки `.env` при обновлении.

| Версия | Коммит | Что добавлено |
|---|---|---|
| 5.1.0 | `cc979bd` | Фоновый раннер форварда к внешнему агенту |
| 5.2.0 | `96ef552` | Эндпоинт `GET /api/v1/chat/limits` |
| 5.3.0 | `9246a7d` | Singleton-lock приложения |
| 5.4.0 | `44bff77` | LLM-профиль `gigachat` |
| 5.5.0 | `5bafbb8` | Rate-limiter сообщений чата (sliding window) |
| 5.6.0 | `e37c920` | Circuit breaker для LLM-провайдеров |
| 5.7.0 | `b4cf6f9` | Per-domain health-эндпоинты |
| 5.8.0 | `96ec85b` | Сквозной correlation_id к внешнему агенту |
| 5.9.0 | `d856f9c` | Метрики выполнения tool'ов и audit-лог чата |
| 5.10.0 | `2b0691e` | Сбор HTTP-метрик (admin) |
| 5.11.0 | `a793770` | Реестр lifespan-хуков и factory-registry |
| 5.12.0 | `7abbed0` | `PollCoordinator` (батчевый поллинг событий агента) |
| 5.13.0 | `858be7c` | Продолжение forward-запроса после перезагрузки страницы |

## 6.0.0 · `2b9e95b`

**MAJOR: редизайн стриминга чата.** Источник истины ответа стал `chat_messages.status` в БД вместо потокового
канала; публичный эндпоинт `GET .../agent-request/{rid}/stream` удалён.

| Версия | Коммит | Что добавлено |
|---|---|---|
| 6.1.0 | `03c93fb` | Аудит отказов в доступе |
| 6.2.0 | `466513d` | `/admin/diagnostics` со статусом батчеров |
| 6.3.0 | `f70544e` | Security response headers (CSP, HSTS и т.п.) |

## 7.0.0 · `7143b66`

**MAJOR: унификация REST-контракта.** Все list-эндпоинты переведены на `PaginatedResponse`, унифицирован
error envelope (`AppError.to_envelope()`) — формат ответа изменён во всех доменах разом.

| Версия | Коммит | Что добавлено |
|---|---|---|
| 7.1.0 | `148749f` | `ActsBroadcast` — cross-tab синхронизация актов |
| 7.2.0 | `ea8e814` | Cache-busting статики (`versioned`-фильтр) |
| 7.3.0 | `8c34afb` | Разделы `/admin/diagnostics` и `/admin/audit-log` в UI |
| 7.4.0 | `e6e3f32` | Отчёты об ошибках фронта (error-boundary) |
| 7.5.0 | `c5172c4` | Строка-маркер `AppendixRef` («В соответствии с приложением») |
| 7.6.0 | `c3f8845` | Подвиды таблиц: налоговые и прочие риски |
| 7.7.0 | `dad0ca3` | Native ES Modules — редизайн загрузки фронтенда (без слома контрактов) |

## 8.0.0 · `a9c9ebb`

**MAJOR: экспорт — контракт изменён.** Подсистема DOCX-экспорта заменена; `POST /save-act` вместо тела
`ActDataSchema` теперь требует query-параметр `act_id`, дефолт формата сменён `txt` → `docx`.

| Версия | Коммит | Что добавлено |
|---|---|---|
| 8.1.0 | `aa508c5` | Многоуровневый рубрикатор DOCX |
| 8.2.0 | `c38e67c` | Нативные footnotes в DOCX |
| 8.3.0 | `8a207bb` | Builder таблиц DOCX |
| 8.4.0 | `1adf22c` | Гиперссылки в DOCX |
| 8.5.0 | `18208a7` | Раздел 6 «Process Mining» (опциональный) |
| 8.6.0 | `e90898e` | Пагинация списков актов, пользователей, истории чата |

## 9.0.0 · `2856353`+`1fe2354`+`7c9638c`

**MAJOR: транспорт чата заменён.** SSE-стриминг заменён на поллинг (`POST /messages` → `GET /messages/{id}`);
3-табличный мост к внешнему агенту снесён.

| Версия | Коммит | Что добавлено |
|---|---|---|
| 9.1.0 | `5097b16` | Тумблер «База знаний ОАРБ» (off/adaptive/always) |
| 9.2.0 | `53c829b` | Fast-fail на GigaChat, `llm_health_probe` |
| 9.3.0 | `46737e1` | Персистентные ширины колонок таблицы |
| 9.4.0 | `9318578` | Предпросмотр листом A4 (WYSIWYG) |
| 9.5.0 | `4e251fd` | Панель замечаний валидации таблиц |
| 9.6.0 | `14760de` | Домен `notifications` (backend) |
| 9.7.0 | `82b8d40` | Единый центр уведомлений на портале и в конструкторе |
| 9.8.0 | `c1dcf78` | Оценка ответов ассистента (лайк/дизлайк) |
| 9.9.0 | `2945db6` | Админ-аналитика обратной связи чата |
| 9.10.0 | `f8ca7b9` | Markdown-рендеринг сообщений чата |
| 9.11.0 | `30c975e` | Стриминг рассуждений агента, idle-таймауты поллера |
| 9.12.0 | `613084d` | Публичный эндпоинт лимитов контента актов |
| 9.13.0 | `313644e` | Картинки нарушений в DOCX-экспорте |
| 9.14.0 | `17de6a2` | Единый per-act снимок черновика с восстановлением |
| 9.15.0 | `ba1a3b3` | Полные данные и картинки в превью нарушений |
| 9.16.0 | `a8b3d66` | Откат удаления блоков (undo-delete, Ctrl+Z) |
| 9.17.0 | `c4b61bf` | Enforce-режим CSP с per-request nonce |
| 9.18.0 | `d4e0d5d` | Копирование узлов между актами |
| 9.19.0 | `f399363` | Статус валидации акта (validation_status) |
| 9.20.0 | `fa4f50c` | Сводка «актов, требующих внимания» на лендинге |
| 9.21.0 | `7fd7a31` | Домен `sqlagent` (портал-страница) |
| 9.22.0 | `fc1f791` | Шапка акта (cover-блок) в предпросмотре |
| 9.23.0 | `62db0c1` | Опциональный раздел Process Mining (доработка) |
| 9.24.0 | `6785f4e` | Внутренний эндпоинт уведомлений для sidecar-агентов |
| 9.25.0 | `b99c7a6` | Межстрочный интервал текстблока, сквозная нумерация сносок |
| 9.26.0 | `2c0d01e` | Ядро generic-табличного тулкита |
| 9.27.0 | `e17a5ff` | UI-оркестратор generic-таблицы |
| 9.28.0 | `88c3655` | Серверная фильтрация/сортировка для ЦКФР |
| 9.29.0 | `3bec183` | ЦК Клиентский опыт на generic-таблице |
| 9.30.0 | `208aa1c` | Целостность капсул текстблока (MutationObserver-страховка) |
| 9.31.0 | `11c8b73` | Многоколоночная сортировка таблицы |
| 9.32.0 | `e2413b0` | Типизированный контракт фильтра/сортировки таблицы |
| 9.33.0 | `23bb3e8` | Round-trip капсул через буфер обмена |
| 9.34.0 | `ce893e4` | Телеметрия здоровья редактора текстблоков |
| 9.35.0 | `de38b2e` | Групповая модель ЦКФР (развёртка по ТБ) |
| 9.36.0 | `fae4330` | Модалка распределения суммы по ТБ |

## 10.0.0 · `50b9123`

**MAJOR: ЦКФР — контракт изменён.** Построчный CRUD (`POST /records`, `POST /records/batch-update`,
`DELETE /records/{id}`) удалён, заменён групповыми эндпоинтами; `POST /records/search` сменил семантику.

| Версия | Коммит | Что добавлено |
|---|---|---|
| 10.1.0 | `91e1904` | Показатель MPL/NPL 90+ |
| 10.2.0 | `93561cc` | Типовые фильтры тулкита (мультивыбор, диапазоны, чипы) |
| 10.3.0 | `152e56a` | Движок поиска/замены по текстблокам |
| 10.4.0 | `35d6676` | Серверные per-node лимиты нарушений и таблиц |
| 10.5.0 | `8527ab2` | Единый конвейер вставки картинок в нарушения |
| 10.6.0 | `1fbbc8c` | Фактуры в версионировании акта (снимок/дифф) |
| 10.7.0 | `ad17fe1` | Клавиатурная перестановка узлов дерева |
| 10.8.0 | `e27ffa5` | Интеграция с ИИ-агентом формулировок: корректор текста |
| 10.9.0 | `0e55ce5` | Корректор: режим «улучшение читаемости» |
| 10.10.0 | `0490557` | ИИ-фича «формализация нарушения» |
| 10.11.0 | `2da3c01` | Поле карточки нарушения «Принятые меры» |
| 10.12.0 | `9cf4f09` | Переработка сохранения/настроек акта (Ctrl+S/Ctrl+Shift+S) |
| 10.13.0 | `ab7fc24` | Дедупликация версий акта по `content_hash` |
| 10.14.0 | `756960d` | Rich-рендер полей нарушения в DOCX/MD/TXT |
| 10.15.0 | `150be2f` | Модуль «богатого форматирования»: поля нарушений на rich contenteditable |
| 10.16.0 | `9630405` | Rich-редактор в подписи под фото |
| 10.17.0 | `50ab9e0` | Rich-редактор в пунктах описания нарушения |
| 10.18.0 | `bff7695` | Rich-поля нарушений — цели поиска/замены |

## 11.0.0 · `b9cd979`

**MAJOR: авторизация — механизм заменён.** ОТП по e-mail + JWT + Redis-сессии заменили stateless-auth по
`JUPYTERHUB_USER`; выпилены JupyterHub/proxy-пути. Вместе с этой волной — email-подсистема уведомлений (`d547de1`).

| Версия | Коммит | Что добавлено |
|---|---|---|
| 11.1.0 | `181faac` | Карточка пользователя (hover-превью, страница профиля) |
| 11.2.0 | `4055b39` | Фото профиля (аватар) |

## 12.0.0 · `1c7feac`+`58d3c14`

**MAJOR: Redis обязателен.** Redis стал обязательной инфраструктурной зависимостью (fail-fast на старте,
in-memory бэкенд локов удалён); блокировки актов переехали с колонок Greenplum на Redis TTL.

## 13.0.0 · `1c28c75`

**MAJOR: connection-per-operation.** Пересмотрена модель соединений с БД (`DbExecutor`) — соединение берётся
только на время SQL-вызова или явной транзакции, а не на весь запрос целиком. Затронуло 77 файлов и весь слой
доступа к БД; формально REST-контракт и конфигурация не изменились, но это фундаментальный редизайн модели
соединения с БД.

- 13.0.11 · `03d510aa` — PATCH: возвращён дефолт `AUTH__COOKIE_SECURE=false` для ПРОМ-конфигурации (SDP —
  обычный HTTP без TLS-прокси, `true` тихо ломал вход через отбрасываемую Secure-cookie).

## 14.0.0 · `0314a6c2`

**MAJOR: профили LLM заменены маршрутами, добавлен транспорт redis-bridge.** `CHAT__PROFILE` больше не
перечисление провайдеров (`sglang`/`openrouter`/`openai`/`gigachat`), а маршрут вида
`<транспорт>,<проводной формат>`: допустимы `openai`, `gigachat`, `redis-bridge,openai`,
`redis-bridge,gigachat` (то же для `CHAT__FALLBACK_PROFILE`). Старые значения `sglang` и `openrouter`
невалидны — конфигурация с ними падает на старте. Появился транспорт `redis-bridge`: запрос к LLM уходит
не по HTTP, а через Redis-стримы к воркеру в Jupyter DataLab, у которого есть сетевой доступ к моделям
(`app/domains/chat/services/redis_bridge_adapter.py`, воркер — `scripts/llm_redis_worker.ipynb`).
Шаблон окружения разделён: `.env.example` → `.env.prod` плюс отдельный `.env.dev`.

- 14.0.1 · `483afa89` — PATCH: документация сверена с кодом. Гайд-бук разбит на хаб и пять частей
  (`architecture-and-backend`, `database`, `ai-assistant`, `testing`, `deploy-and-configuration`),
  deep-dive по актам влит в `data-model-acts.md`; убраны JupyterHub-деплой, следы SSE и упразднённые
  профили LLM.
- 14.0.2 · `470c319d` — PATCH: метаданные приложения (`__title__`, `__version__`) сведены к единственному
  источнику `app/__init__.py`, откуда их берёт `Settings`. `APP_TITLE`/`APP_VERSION` в шаблонах `.env`
  закомментированы (env-override остался рабочим), из документации убран хардкод версии, согласованность
  проверяет `tests/test_version_consistency.py`.
- 14.0.3 · `0c4d472c` — PATCH: наименование продукта в коде и UI сведено к двум канонам — `Audit
  Workstation` (код, доки, API-метаданные) и «Единое рабочее место» (интерфейс пользователя); убраны
  разошедшиеся синонимы («Аудиторская рабочая станция» на странице входа, «Портал инструментов» на лендинге).
  Ноутбук воркера LLM-моста перенесён из `scripts/datalab/` прямо в `scripts/`.
- 14.0.4 · `48ff9fa2` — PATCH: `insert_question` (канал к внешнему агенту) больше не пишет SQL NULL в
  `media`/`buttons` — владелец bus-таблицы держит их NOT NULL без DEFAULT. `buttons` у вопроса всегда пустой
  массив (кнопки бывают только в ответе агента). `media` при отсутствии вложения — пустой объект, а не пустой
  массив: колонка на стороне владельца хранит и объект, и массив вперемешку (`map_answer_to_blocks` уже
  разворачивает единичный объект в список).

## 15.0.0 · `39418fff` (+`40bfd867`)

**MAJOR: блочная модель нарушения — модель хранения переработана.** Поля нарушения перестали быть
текстом и «списком пунктов»: каждое из десяти полей (`violated`, `established`, `description`,
`codeMining`, `processMining`, `additionalContent`, `reasons`, `measures`, `consequences`,
`responsible`) — единый JSONB-контейнер `{enabled, blocks}`, где блок бывает трёх типов:
`text` / `image` / `table`. Картинки и встроенные таблицы больше не привилегия отдельных полей, а
обычный блок в любом поле; порядок полей внутри нарушения задаёт пользователь, он хранится в
`fieldOrder`. Источник истины — реестр `app/domains/acts/violation_fields.py`, от которого
производятся схема `ViolationSchema`, полевые колонки таблицы, санитайзер, DOCX/MD/TXT-рендеры,
дифф версий и аудит-отпечаток; фронтовое зеркало реестра пиннится тест-стражами.

Схема `act_violations` изменена несовместимо: `violated`/`established` TEXT → JSONB,
`description_list` → `description`, добавлены `code_mining`, `process_mining` и `field_order`,
GIN-индекс перестроен на все полевые колонки. `CREATE TABLE IF NOT EXISTS` существующую таблицу не
трогает — **развёрнутую базу нужно мигрировать вручную**.

Вместе с этой волной изменён контракт формализатора (`d51f9db9`, `4eeb514e`): `FormalizeResponse`
отдаёт готовый HTML, а не сырой текст с фронтовой эвристикой, а отказ LLM виден пользователю как
503 вместо пустого успешного ответа. Маршруты LLM выбираются по реально доступным провайдерам, и
текстовые действия (корректор, формализатор) ходят по тому же плану маршрутов, что и чат, а не по
`CHAT__PROFILE` напрямую.

- 15.0.1 · `7617ad7d` — PATCH: карточка файла в чате понимает инлайн data-URL в `file_id`. Внешний бот
  (nanobot) отдаёт вложение телом прямо в блоке, а не ссылкой на загруженный файл, и прежний рендер уводил
  такой `file_id` в `/api/v1/chat/files/{id}`, где поиск по UUID в `chat_files` давал 404. Имя файла
  восстанавливается из текста ответа или `mime_type`, когда бот не передал `filename`. Иконки файлов
  вынесены из inline-SVG в `static/icons/chat/*.svg` с явными размерами (без них при закешированной CSS
  выходило 0×0), блок рассуждений свёрнут по умолчанию. Стенд имитации внешнего агента дополнен
  сценарием доставки файла инлайном. Изменения клиентские: бэкенд, схема БД и API не затронуты.

## 15.6.0 · `53e4851e`

**MINOR: файлообмен с внешним агентом доведён до модели «транспорт ≠ хранение» и статусной модели
NanoBot 2.3.** Шина по-прежнему несёт base64 data-URL в обе стороны, но внутри приложения файлы живут
только в `chat_files`: входящие вложения агента материализуются при финализации ответа (идемпотентно,
`uuid5` от `answer_uid` и индекса), в блок сообщения уходит UUID. Горячий опрос шины переведён на узкие
проекции — `media` читается ровно один раз, при финализации. Парсинг `media` стал best-effort: битый
элемент даёт error-блок, но не роняет ответ; `file_id` классифицируется по виду (data-URL / http /
UUID / прочее), http(s)-ссылки агента фронт отдаёт как есть — без `?inline=true` и без auth-заголовков
на чужие хосты, и рендерит file-блоком (CSP ПРОМа блокирует внешние картинки). Статус `error` стал
повторяемым (агент ретраит сам), терминалом остался `failed`; reclaim `processing→pending` — признак
жизни с капом откатов по эпизодам; `answer_timeout_sec` поднят до 30 минут. CHECK-констрейнты шины
расширены (`role` +`tool`, `status` +`error`) — файл миграции
[`2026-08-19-bus-status-check-error.md`](docs/migrations/2026-08-19-bus-status-check-error.md).
Лимиты подняты под большие вложения: `CHAT__AGENT_CHANNEL__MAX_MEDIA_FILE_SIZE` (новый, 512 МБ),
`CHAT__MAX_FILE_SIZE`/`MAX_TOTAL_FILE_SIZE` — 512 МБ, `SECURITY__MAX_REQUEST_SIZE` — 520 МБ; страж
размера входящего вложения считает по оценке payload, а не по цифре, объявленной агентом. base64 и
JSON больших payload'ов уведены в worker-поток и за пределы удерживаемого соединения.

## 15.5.0 · `80fafe25`

**MINOR: наработки D17 — улучшайзер «Пиши, сокращай» и полный конвейер формализатора.** Промпт
читаемости заменён вариантом команды D17 (температура 0.1), перенесён их анализатор читаемости
(`readability_analyzer.py`): восемь метрик, формулы штрафов и пороги светофора 30/60 как в исходнике,
морфология на `pymorphy3` (результаты сверены пословно на корпусе D17). `POST /text-actions/correct`
в режиме `readability` отдаёт диагностику `readability: {before, after}`; в панели корректора над
сравнением «было/стало» появился светофор с изменившимися метриками — сбой анализатора скрывает
светофор, но правку текста не роняет. Формализатор переведён на шесть промптов нового дампа D17 и
стал полным конвейером: четыре экстрактора параллельно, затем сборщик per-field JSON и рекомендации
тоже параллельно, с фолбэком на сырые экстракторы при сбое сборщика. Появилось поле «Описание» —
метрики нарушения побулитно расшифровывают сказанное в «Установлено».

## 15.4.0 · `23fcbe90` (+`47e964d6`)

**MINOR: вложения уходят в шину внешнего агента байтами, а не ссылкой.** Строка-вопрос в
`chat_agent_messages_bus` теперь несёт файлы в формате владельца шины: элемент
`{file_id, filename, mime_type, file_size}` **без** дискриминатора `type`, где `file_id` —
`data:<mime>;base64,<payload>`. Раньше туда as-is уезжали блоки формата `chat_messages.content`
— с лишним `type` и с UUID вместо payload'а, так что читатель шины не получал сами байты.
Конверсия собрана в одну точку (`build_bus_media_from_file_blocks` в
`services/agent_channel.py`), её зовут оба пути форварда: режим «Всегда» в `api/messages.py`
и forward-тул в `services/agent_loop.py`. Потеря одного файла не роняет вопрос целиком: блок
без `file_id` или отсутствующий в `chat_files` пропускается с warning'ом. Хранение не
изменилось — файл по-прежнему лежит в `chat_files`, а история чата в `chat_messages.content`
остаётся в UUID-формате, поэтому иконки и скачивание на фронте работают как раньше. Добавлен
гайд [`chat-files-data-requirements.md`](docs/guides/chat-files-data-requirements.md) —
требования к данным чата в Greenplum и контракт `bus.media` в обе стороны.

Схема БД, конфигурация и REST-контракты AW не менялись — калибр MINOR за то, что меняется
форма данных, которую получает внешний потребитель шины.

> **Тяжёлые вложения в режиме агента стали дороже.** Строка-вопрос теперь хранит байты, а
> `poll_once` перечитывает её целиком каждый тик (2–10 с, до 10 минут). При потолке
> `CHAT__MAX_TOTAL_FILE_SIZE` (30 МБ → ~40 МБ base64) это заметный трафик на пул из двух
> соединений. Сужение выборки поллера вынесено в отдельную задачу.

- 15.4.1 · `2051e567` — PATCH: Playwright-сьюта снова полностью зелёная (173 passed вместо
  166 passed / 7 failed). Все семь падений были на стороне тестов, приложение исправно.
  Спеки 24/25/26 наполняли поле нарушения через `поле.__surface`, то есть по до-блочной
  модели: сейчас поле — контейнер `{enabled, blocks}`, а rich-редактор живёт внутри блока,
  поэтому у поля без блоков его нет вовсе — сид переведён на `seedViolationBlocks`, чтение
  модели идёт через `blocks[].content`, id целей поиска — `viol:{vid}:{field}:block:{blockId}`;
  мёртвая `seedViolationField` удалена. Хелпер `openAct` ждал дерево видимым, хотя
  `_restoreViewPosition` штатно возвращает повторно открытый акт на сохранённый шаг 2, где
  `#step1` скрыт целиком — теперь ждёт дерево в DOM (спека 29). Спека 18 ждала диалог
  восстановления черновика после `Ctrl+S`, хотя снимок пишется только при несохранённых
  правках и вытесняется успешным сейвом — ожидание заменено на терпимое
  `discardLocalDraftIfPrompted`, поднятое в общие фикстуры. Изменения только в
  `tests/playwright/`: код приложения, схема БД и API не затронуты.

---

## 15.3.0 · `08e1f627`

**MINOR: честный статус сохранения, черновики актов и целостность навигации.** Индикатор
«не сохранено» перестал загораться от кликов: `currentStep`/`selectedNode`/`selectedCells` убраны
из отслеживаемых свойств `AppState` — это UI-состояние, в `exportData()` оно не входит. Шаг и
позиция скролла конструктора переживают перезагрузку и смену акта (`view-position-store.js`, ключ
per-act `audit_workstation_viewpos:{actId}`), пишутся в обход dirty-трекинга. Сохранение контента
получило optimistic-проверку: `acts.content_version` инкрементируется только записью **содержимого**,
фронт эхом возвращает его в необязательном `expected_content_version`, расхождение → **409
`content-conflict`** вместо слепой перезаписи чужих правок; правки метаданных и сохранение соседней
части КМ ложных конфликтов не дают. Конфликт локального черновика с версией БД решает пользователь
диалогом, а не молчаливое удаление; Escape прячет черновик в бэкап
(`audit_workstation_conflict:{actId}`), удаление — только по явному ответу про него. Навигация:
у `popstate` остался единственный владелец, смена акта переносит лок (`unlock` старого + `init`
нового), первая запись `history.state` несёт `actId`, переключения сериализованы общей очередью —
удержанный «Назад» и клик в меню поверх перехода больше не снимают лок дважды. Выходной save
адресует показанный акт, уважает dirty-эпоху и честно сообщает о сбое вместо тишины.

**Правило эволюции схемы дополнено:** ломающее изменение (переименование, смена типа, удаление)
по-прежнему означает пересоздание БД — легаси не держим; расширяющее (колонка, индекс, констрейнт)
доносится файлом `docs/migrations/YYYY-MM-DD-<таблица>-<поле>.md` со скриптами под PG и GP. Формат —
§6.5.5 в [`database.md`](docs/guides/database.md).

> **Требует действия на уже развёрнутых БД.** Колонка `acts.content_version` добавляется скриптом
> из [`docs/migrations/2026-08-19-acts-content-version.md`](docs/migrations/2026-08-19-acts-content-version.md).
> Без наката приложение стартует, но открытие любого акта падает `UndefinedColumnError` — колонка
> перечислена в `SELECT` явно. Бэкфилл не нужен: `DEFAULT 0` — валидное стартовое значение.
> На чистой БД накатывать не надо, `create_tables_if_not_exist` создаст таблицу сразу с колонкой.

Обратно совместимо на уровне контракта: `expected_content_version` необязателен — клиент, который его
не шлёт, работает как раньше, проверка просто не делается.

---

## 15.2.0 · `b310d90d`

**MINOR: групповая работа с блоками нарушений и документная типографика правки.** Блоки нарушения
получили мультивыделение (клик по шапке, Ctrl/Shift-набор, подсветка), групповое перетаскивание одной
пачкой и групповое удаление («Удалить N блоков», Delete); перетаскивание начинается только с шапки,
индикатор вставки стал оверлеем и показывается лишь во время переноса. Ctrl+V создаёт блок по фокусу,
а не всегда в конец поля. Списки переведены на собственные indent/outdent вместо нативных команд
браузера: без пустых хостов, с продолжением нумерации, каретка переживает смену уровня и выход из
списка с пустого пункта. Размер шрифта хранится и передаётся в пунктах — выбранный кегль доезжает до
Word без искажения, накладывается поузлово (списки не плодят пустых обёрток), пункту списка разрешён
собственный кегль. Поверхности правки, лист предпросмотра, вкладка сравнения версий и DOCX сведены к
одной документной гарнитуре и одному печатному кеглю: экранный множитель убран, кегль таблиц приведён
к вордовским 9pt, втяжка списков одинакова во всех четырёх поверхностях. Автосейв больше не сбрасывает
редактирование ячейки таблицы (коммит значения без blur). Обратно совместимо: REST-контракты, схема БД
и конфигурация не менялись; легаси-контент в пикселях конвертируется санитайзером в пункты (×0.75),
размеры старых актов не смещаются.

---

## 15.1.0 · `2cb9e9ed`

**MINOR: многоуровневые списки в rich-редакторе.** Списки в текстблоках и rich-полях нарушений
стали многоуровневыми (до 9 уровней): Tab/Shift+Tab и пункты меню «Уровень глубже/выше» меняют
уровень пункта, маркеры идут по схеме «как в Word» (• ◦ ▪ и 1. / a) / i., цикл по 3 уровня)
согласованно в редакторе, превью и DOCX. В DOCX-экспорте каждый `<ul>/<ol>` получает собственную
пару `w:abstractNum`+`w:num` вместо built-in стилей Word — соседние списки больше не связаны ни
сквозным счётом, ни подсветкой «одним семейством» при клике по маркеру. Тулбар редактора схлопнут
в дропдауны (выравнивание, списки, размер шрифта) и всегда помещается в одну строку; добавлена
кнопка «Очистить форматирование» (ластик), снимающая помимо inline- и блочное форматирование
(списки, blockquote, выравнивание, отступы) строго в границах выделения. Вложенные списки в
MD/TXT-экспорте получают CommonMark-безопасный отступ 4 пробела на уровень. Обратно совместимо:
REST-контракты, схема БД и конфигурация не менялись.

- 15.1.1 · `daac61e6` (+`98e32879`) — PATCH: иконки файлов в чате различают формат подписью, а не только
  цветом. Для pdf/docx/xlsx/pptx/md/txt добавлены отдельные `static/icons/chat/*.svg` с текстовым ярлыком
  внутри корпуса; остальные форматы остались на общих иконках по форме — generic, spreadsheet, presentation,
  image, code, archive. Карта `_ICON_FORM` в `chat-renderer.js` переписана под это: csv переведён на иконку
  таблицы и перекрашен в зелёный к xls/xlsx, md — в синий к docx. Ширина четырёхбуквенных ярлыков
  зафиксирована через `textLength`, иначе подпись упирается в обводку корпуса и разъезжается там, где
  вместо Arial подставится другой шрифт. Изменения клиентские: бэкенд, схема БД и API не затронуты.
- 15.1.2 · `95c8ddb2` (+`7a1b4667`) — PATCH: визуально корявые `file-spreadsheet-24.svg`,
  `file-presentation-24.svg` и `file-archive-24.svg` удалены с диска, вместо них — тематические
  `csv.svg`, `ipynb.svg`, `py.svg` и `zip.svg`; xls/csv/ppt временно идут на `csv.svg`, все архивы —
  на `zip.svg` (WinRAR-стиль). `py.svg`/`zip.svg` рисуются фирменными цветами и игнорируют
  `currentColor`. Докстринг `_getFileIconSvg` приведён в соответствие с тремя видами иконок.
  Изменения клиентские: бэкенд, схема БД и API не затронуты.
- 15.1.3 · `c9449d14` — PATCH: вставка в подписи пунктов структуры очищается до чистого текста.
  Ctrl+V и перетаскивание в подпись узла дерева проходят через `insertText`: разметка, стили и переводы
  строк из буфера отбрасываются, многострочный источник схлопывается в одну строку. Сброс файлов в подпись
  гасится (браузер иначе открывал файл во вкладке), перенос текста внутри самой подписи и перетаскивание
  узла дерева оставлены браузеру и дереву. Попутно `DialogBase` стал идемпотентен на закрытие: повторный
  `hide()` не ставит второй таймер, а `show()` отменяет незавершённое закрытие — кнопка помощи снова
  открывается после закрытия крестиком, её статическая нода больше не удаляется из DOM. Дубль
  автоинициализации `HelpManager` на `DOMContentLoaded` убран — владелец один, `app.js`.
  Изменения клиентские: бэкенд, схема БД и API не затронуты.

---

## 16.0.0 · `700a3dd4`

**MAJOR: Process Mining стал обязательным разделом скелета — акт прежней модели невалиден.** Раздел
перестал быть необязательным дополнением, которое пользователь добавляет из меню: он входит в базовую
структуру наравне с разделами 1–5, создаётся при инициализации акта для обоих типов проверки и защищён
от удаления, переименования и перетаскивания (`protected` + `deletable: false` + `titleLocked`).
Валидация базовой структуры требует корневые разделы `1`–`6` с обеих сторон — `ValidationAct.validateStructure`
на фронте и `collect_content_issues` на бэке. Акт, сохранённый без этого раздела, структуру не проходит:
экспорт блокируется, статус акта уходит в `error`, а дописать раздел из интерфейса нельзя — прежняя точка
добавления убрана вместе с необязательностью. Легаси-акты не поддерживаются сознательно: проект их не
держит. Вместе с этим спец-флаги узла (`special`, `titleLocked`) добавлены в `ActItemSchema` и переживают
сохранение — без них маркер раздела терялся при первой же перезагрузке.

**Предпросмотр сведён с Word.** Кегль и вертикальный ритм листа сняты с интерфейсной rem-шкалы и переведены
на документные токены: заголовки пунктов идут одним печатным кеглем на любой глубине, воздух между блоками
задают `--doc-space-after` и `--doc-blank-line`, ступеней по уровню заголовка больше нет. В лист добавлены
плашка-рубрикатор раздела и блок подписи руководителя, данные в ячейках центрируются, поля ячеек приведены
к вордовским. Подписи таблиц оформляются по единому правилу: подчёркиваются только пресетные таблицы
разделов 1–4, жирного нет нигде, кегль подписи печатный наравне с телом листа. Word остаётся эталоном —
расхождения чинились на стороне превью.

**Таблица «Результаты оценки качества процесса»** в разделе 2 стала удаляемой и создаётся заново из
контекстного меню. **Замечания о незаполненности стали тише**: рамка недозаполненной таблицы на листе —
тонкая пастельная из палитры вместо ярко-оранжевой (рамка структурного дефекта намеренно осталась
заметной, её причина ломает экспорт), а однотипные замечания в колокольчике сворачиваются в одну
раскрывающуюся группу с переходом к каждой таблице; счётчик показывает число групп.

Схема БД и конфигурация не менялись; в REST-схеме узла добавлены необязательные `special` и `titleLocked`.

---

## 17.0.0 · `c0783083`

**MAJOR: картинки нарушений вынесены из JSONB в таблицу `act_images` — модель хранения изменилась, БД
пересоздаётся.** Блок картинки больше не носит байты в себе: он хранит `image_id`, ссылку на строку
`act_images` (`id` VARCHAR(36), `data` BYTEA, `UNIQUE (act_id, content_hash)`, FK с CASCADE). Снимок
версии акта теперь ссылается на картинку, а не копирует её, поэтому полсотни версий перестали
множить одни и те же байты; неиспользуемые строки собирает GC (`collect_live_image_ids` →
`delete_unreferenced`). Проверка «картинка пуста» в `content_validation.py` смотрит на `image_id`
вместо `url`. Легаси-данных проект не держит: развёртывание идёт через
[`drop-all-tables.md`](docs/migrations/drop-all-tables.md) и `create_tables_if_not_exist`. Вторая
причина мажора — переименован ключ конфигурации: `SECURITY__MAX_TRACKED_IPS` стал
`SECURITY__MAX_TRACKED_CLIENTS`, потому что лимитер считает клиентов, а не адреса.

**MIME определяется по байтам, а не по заголовку формы.** Тип картинки распознаёт Pillow
(`_PILLOW_FORMAT_TO_MIME`), `Content-Type` из multipart игнорируется, имя файла только логируется —
поэтому ни длинное бинарное имя, ни поддельное расширение ни на что не влияют. SVG не принимается
сознательно (это исполняемый документ, а не растр); ответ отдаётся с `X-Content-Type-Options: nosniff`,
загрузка читается потоком по 256 КБ. Фронт переведён на ссылочную модель и WebP; режим «Исходное»
тоже сжимает — в собственном формате файла и только если результат вышел меньше оригинала, иначе
уходят исходные байты. Снимки версий перестали оставаться сиротами на Greenplum.

**Лимиты подняты под многопользовательскую нагрузку**, и лимитер перестал наказывать за загрузку
страницы: статика в счёт не идёт, а ключом стало имя пользователя из проверенной подписи JWT вместо
адреса — за общим NAT пользователи больше не делят один счётчик. `X-Forwarded-For` сознательно не
читается: без списка доверенных прокси заголовок подделывается одной строкой, что превратило бы
лимит в формальность. Что понадобится, если прокси появится, описано в §9.5d
[`deploy-and-configuration.md`](docs/guides/deploy-and-configuration.md). Пул БД намеренно НЕ
поднимался — у ПРОМ-учётки GP лимит около пяти соединений.

**Открытая вкладка узнаёт о новой версии.** Версия приложения переехала в путь статики
(`/static/v17.0.0/js/…`) — релиз меняет URL, и браузер обязан взять новый файл; вечный кэш включается
флагом `SECURITY__STATIC_IMMUTABLE` (на ПРОМе год, в DEV выключен). Давно открытая вкладка опрашивает
`/api/v1/system/version` и предлагает обновиться баннером. Программно сделать «жёсткую» перезагрузку
нельзя: `location.reload(true)` работает только в Firefox, а Service Worker и `Clear-Site-Data`
требуют secure context, которого на ПРОМе (обычный HTTP) нет.

**Бюджет ответа и таймаут text-actions считаются от длины входного текста**, а не берутся константой —
раньше длинный текстблок обрывался на середине. Оборванный ответ (`finish_reason == "length"`) отдаётся
ошибкой, а не подставляется в текстблок вместо исходного текста; число повторов ограничено, чтобы
неудачная попытка не умножала таймаут.

Три env-файла синхронизированы, мёртвые параметры убраны. Появились два стража, строящие ожидание
прямо из pydantic-моделей: расхождение `.env.dev`/`.env.prod` с моделями настроек и расхождение
реестра переменных в §9.5 доков с дефолтами моделей — новое поле настроек автоматически становится
требованием к обоим.

- 17.0.1 · `a16c3cec` — PATCH: табличное замечание перестало приходить в колокольчик дважды — плоско от
  серверного источника «validation» и группой от живого источника «tables»; три табличных кода теперь
  подавляются на фронте (`SUPPRESSED_CODES`), на серверный статус акта это не влияет. Проблемы шапки
  таблицы подняты до `error` — акт с битой шапкой больше не проходит гейт экспорта; таблица без шапки
  снова даёт замечание «нет данных» (терялось, потому что сбор упирался в отсутствующий заголовок).
  Убран дублирующий тост при сохранении, удалён неиспользуемый `ValidationTable.validateData()`.

---

## 17.1.0 · `794c5eb8`

**Несколько дефолтных админов через список в конфиге.** `ADMIN__USER_DIRECTORY__DEFAULT_ADMIN` заменён на
`ADMIN__USER_DIRECTORY__DEFAULT_ADMINS` — логины через запятую (`parse_admin_logins`, разбор как у
`CHAT__PROFILE`). При начальном заполнении ролей роль «Админ» получает каждый логин из списка, который
нашёлся в каталоге пользователей, а не только один захардкоженный. На ПРОМе значение — `22494524,18284771`.

**Каталог пользователей на ПРОМе переехал на основную GP-схему.** `ADMIN__USER_DIRECTORY__SCHEMA` в
`.env.prod` больше не указывает на `_project_4` — пустое значение, как в DEV, читает `t_db_oarb_ua_user`
из основной схемы приложения (`_project_34`).

- 17.1.1 · `3d61f1b3` — PATCH: `CHAT__MODEL`/`CHAT__FALLBACK_MODEL` стали опциональны для
  `redis-bridge,*`-маршрутов (дефолт `""` вместо захардкоженного `gpt-4o`) — пустое поле `model` в
  заявке подставляет свой дефолт воркер (`GIGACHAT_MODEL`/`OPENAI_MODEL`). Ячейка запуска
  `llm_redis_worker.ipynb` теперь жёстко проверяет перед стартом прослушки хотя бы одну
  настроенную цель LLM и живой Redis моста — падает с понятной ошибкой вместо «зомби»-воркера,
  который отвечает на heartbeat, но роняет каждую заявку.
- 17.1.2 · `050d8d5b` — PATCH: скрипт дропа справочников (`drop-reference-tables.md`) в GP-секции
  различает захардкоженную схему `_4` (hive/ЦК) и схему, унаследованную от `DATABASE__GP__SCHEMA`
  (UA_DATA/каталог пользователей — обе пустые по умолчанию), раньше описанные одним общим правилом.
  Удалён устаревший файл миграции `2026-08-19-bus-status-check-error.md` — расширенные CHECK'и уже
  объявлены в обеих `schema.sql`.
- 17.1.3 · `7738d99a` — PATCH: документация сверена с кодом по итогам многоагентного аудита —
  исправлены RateLimit-ключ и дефолт в architecture-and-backend.md, число таблиц домена acts в
  database.md, владелец `create_streaming` и модуль перехвата форварда в agent-channel-sequence.md,
  устаревший механизм cache-busting и счётчики файлов/строк в frontend-architecture.md, число
  сценариев спеки 29 в textblock-editor-architecture.md, битая ссылка на чек-лист блока
  конструктора в golden/README.md, отсутствовавшее покрытие `BridgeSchemaError`/`BridgeBackendError`
  в retry-test-scenarios.md.
